from pyvis.network import Network
import pandas as pd
import networkx as nx

# nx_graph = nx.cycle_graph(1001)

# nx_graph.nodes[1]['title'] = 'Number 1'
# nx_graph.nodes[1]['group'] = 1
# nx_graph.nodes[3]['title'] = 'I belong'
# nx_graph.nodes[3]['group'] = 10

# nx_graph.add_node(20, size=20, title='couple', group=2)
# nx_graph.add_node(21, size=15, title='couple', group=2)

# nx_graph.add_edge(20, 21, weight=10)
#
# nx_graph.add_node(25, size=25, lebel='lonely', title='lonely node', group=3)
#
# nx_list = []
# for i in range(1, 1000):
#     nx_list.append((1, i + 1))
# nt = Network('720px', '100%', bgcolor='#222222', font_color="white")
# nt.from_nx(nx_graph)
# nt.add_edges(nx_list)
# nt.toggle_physics(True)
# nt.show(name='nx.html', notebook=False)


